/* ============================================================
   FlowMD Module Namespace — global registry shared across js/
   modules and app.js. Loaded first.
   ============================================================ */
window.FlowMD = window.FlowMD || {};
