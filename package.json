{
  "name": "flowmd",
  "version": "2.0.0",
  "private": true,
  "description": "FlowMD — Plan. Study. Track. Succeed. (NEET-PG Study Planner)",
  "scripts": {
    "version:bump": "node scripts/bump-version.js",
    "deploy:firebase": "firebase deploy --only hosting",
    "deploy": "npm run version:bump && git add -A && git commit -m \"chore: bump cache version\" && git push && npm run deploy:firebase",
    "deploy:quick": "npm run version:bump && npm run deploy:firebase",
    "deploy:all": "npm run version:bump && firebase deploy"
  },
  "dependencies": {
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "libsodium-wrappers": "^0.8.4"
  }
}
