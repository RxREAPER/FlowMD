{
  "name": "flowmd",
  "version": "2.0.0",
  "private": true,
  "description": "FlowMD — Plan. Study. Track. Succeed. (NEET-PG Study Planner)",
  "scripts": {
    "version:bump": "node scripts/bump-version.js",
    "test": "node tests/modules.mjs && node tests/metrics.mjs && node tests/smoke.mjs && node tests/onboarding.mjs && node tests/navigation.mjs && node tests/migration.mjs && node tests/offline.mjs && node tests/render.mjs && node tests/inline-styles.mjs && node tests/scope-leaks.mjs && node --test \"tests/unit/*.test.mjs\"",
    "deploy:firebase": "firebase deploy --only hosting,firestore:rules",
    "deploy": "npm run version:bump && git add index.html sw.js js/core/constants.js && git commit -m \"chore: bump cache version\" && git push && npm run deploy:firebase",
    "deploy:quick": "npm run version:bump && npm run deploy:firebase",
    "deploy:all": "npm run version:bump && firebase deploy"
  },
  "dependencies": {
    "pngjs": "^7.0.0",
    "sharp": "^0.35.3",
    "tesseract.js": "^7.0.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase-tools": "^15.24.0",
    "libsodium-wrappers": "^0.8.4",
    "playwright": "^1.62.1"
  }
}
